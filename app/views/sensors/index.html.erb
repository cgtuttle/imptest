<h2>Current Readings</h2>

	
	<table>
		<thead>
			<th>Sensor Channel</th>
			<th>Reading</th>
		</thead>
		<% @samples.each do |s| %>
			<tbody>
				<td><%= s.sensor_id %></td>
				<td><%= s.value %></td>
			</tbody>
		<% end %>
	</table>
	
